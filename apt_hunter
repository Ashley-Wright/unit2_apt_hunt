#!/usr/bin/env ruby
require_relative 'lib/complex_parse_arguments'

command = ARGV[0]
type = ARGV[1]

options = ComplexParser.parse
error_messages = ComplexParser.validate(options)

if error_messages.empty?
  puts "success"
else
  puts error_messages
end
